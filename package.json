{
  "name": "fieldscout",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check api.js && node --check backup.js && node --check db.js && node --check i18n.js && node --check ranking.js && node --check sw.js && node --check utils.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  }
}
